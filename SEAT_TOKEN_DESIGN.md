# 座位令牌（Seat Token）设计方案 — v1.4.0 候选

> 状态：**主体已实现并验证**（v1.4.0，commit `d88738a`；换边语义于 R5 修订）。
> 实现已完成，本文内容已合并进 `CONSISTENCY.md` 第十一节；
> 本文件保留**决策记录**（§零 R1–R5）与**网页版下线检查单**（§6.1），待下线执行后删除。
> 关联：`CONSISTENCY.md` §9（座位租约）、§9.7（观战者误触发接手面板）、
> §11（座位令牌与换边，实现后的权威描述）、§9.5（脚手架值泄漏）

---

## 零、修订记录

### R5（2026-09-12）— 换边语义纠正：**按人判断** + **换完同时重开**

用户纠正：

> 「我说的是自己下第一步前可以申请换边，也就是说白方走了第一步，黑方依然可以申请换边，
> 换完后应该同时重开。」

#### 我错在哪

R4 之后的首次实现里，我把换边语义写成「**双方都还没走过一步**」，
于是：

1. 判据用 `this.game.history().length > 0` —— 这是**盘面总步数**，不是"我自己的步数"。
2. 后果一（**功能被堵死**）：白方走 1.e4、黑方申请时，黑方自己显然 0 步，
   但 `history.length === 1 > 0` → 黑方的**合法请求被误拒**。用户举的例子恰好被排除。
3. 后果二（**接收侧判据方向反了**）：接收侧写 `if (that.game.history().length > 0) reject`，
   即"**我（接收方）**走过棋就拒绝"。白方走 1.e4 后，白方（接收方）手里 `n=1`
   → 即便请求方是黑方，也会被这个判据拦下。
4. 后果三（**违反用户明确要求**）：`executeSwap()` 不重开棋局。用户要求"同时重开"。

#### R5 的正确口径

`history` 是**半回合序列**（白走第 1/3/5…、黑走第 2/4/6…），故"**我自己**走过几步"为：

| 座位 | 等价条件 |
|---|---|
| 白 | `history.length === 0` |
| 黑 | `history.length ≤ 1` ← **含"白方已走 1 步"** |

→ 白方 1.e4（n=1）后，黑方自己 0 步 → **可申请** ✅（用户原话）。

**"换完同时重开"是逻辑必然而非附加要求**：换边后新白方 = 原黑方，
而原黑方在那个局面里本是后手；让他执白先行、盘上却压着旧白方走过的那一步，
白先行的不变量被破坏，双方走子数必然失衡（新白方 0 步 / 新黑方 1 步）。
`reset()` 清盘后永远是"新白方在空盘上先行" → **任何**换边都自洽。

#### 我一度引入、随后自己否掉的设计（留档，避免重犯）

R5 中途我给 `_canSwapNow()` 加了「必须轮到我走」的**轮次守卫**，理由是
"我走了 1 步、对方 0 步时换边会失衡"。但**重开已经消灭了失衡的可能** ——
该守卫只会额外挡住"黑方在第 0 步请求换边"这种完全无害的情形（黑方 0 步、
轮不到他，但换边 + 重开后完全合法）。**守卫收敛为"自己 0 步"一条**，
与用户语义一一对应。

#### 可执行取证（新增）

| 脚本 | 作用 | 结果 |
|---|---|---|
| `verify-swap-r5.js` | 把 R5 的三个 bug **注回**代码，验证 W 段断言确有鉴别力 | **15 / 15** |
| `probe-index-page.js` W 段 | 换边用例（含"白方 1.e4 后黑方可申请"） | 全量 **238 / 238** |

注入 → 转 FAIL 的对应关系：

| 注入 | 内容 | 转 FAIL |
|---|---|---|
| A | 接收侧判据改回"按**接收方**步数算" | W4 / W4b / W4c / W15h |
| B | `executeSwap()` 删掉 `game.reset()` | W7f / W7f-b / W7f-c / W9c-c / W11g / W15e / W15f |
| C | `_canSwapNow()` 改回"双方都零步" | W1c / W3c / W11f / W11g |

### R4（2026-09-12 20:10）— 用户确认「网页版确定不管了」；**修正 R3 的一处错误结论**

用户决策：

> 「网页版确定不管了。」

**选定分支 B**（网页版计划下线，非长期共存）。但「不管了」是**态度**，
不是**运行时事实** —— 已实测：

| 核查项 | 结果 |
|---|---|
| `https://chess.yiruifu.cn/` | **HTTP 200，仍在线** |
| 线上版本 | `APP_VERSION = '1.3.8'` |
| 线上是否含令牌字段 | **0 处**（`seatToken`/`seatClaimAt`/`chessSeatTokens` 全无） |

→ **只要域名还通，网页版就是一个可用客户端，别人随时能以此身份进房。**

#### ★ R4 自纠：R3 的 §5.2 结论是**错的**

R3 时我写「`!reqIsMyOpponent` 不能在首版删除，须作为对端无令牌时的兜底保留」。
**这条是错的，且错得有害。** 重新逐行核对 v1.3.8 判据后确认：

```js
var reqIsMyOpponent = iAmPlayer && requesterSeat === oppColor();
if (iAmPlayer && hasHistory && requesterIsPlayer && !reqIsMyOpponent && ...) { 弹面板 }
```

**只有两个座位，我占一个，另一个必然就是 `oppColor()`。**
所以「`requesterIsPlayer` 且 `!reqIsMyOpponent`」= 请求方坐在**我自己的座位**上 ——
而「抢空位」场景里，请求方坐的**必然是空出来的那个（我的对手位）**，
于是 `reqIsMyOpponent` **恒为 true** → `!reqIsMyOpponent` **恒为 false** →
**面板恒不触发**。

**这正是 §4.6 判定它为"死代码"、§1.2 实验（`repro-silent-takeover.js`）证实静默接手的同一个原因。**

**后果**：保留 `!reqIsMyOpponent` **不会**给网页版场景提供任何兜底，
只会把 **MP ↔ MP 的保护一起废掉**（面板永不触发）。
「保留它」的实际效果不是"降级为较弱保护"，而是**整体归零**。

→ **§4.6 原指示（删除）正确，R3 的 §5.2 撤回。** 见 §5.2（已重写）。

#### R4 的实质结论

| 场景 | 保护 |
|---|---|
| MP v1.4.0 ↔ MP v1.4.0 | ✅ 完整 |
| MP v1.4.0 ↔ 网页版 / 老版本 MP（无令牌） | ❌ 无面板（**等同 v1.3.8 现状，无回归**） |

**网页版场景无保护，这一点无法通过保留任何旧守卫来改善** ——
只能靠**下线网页版**来消除该场景。故 B 分支不只是取舍，**是唯一出路**。

#### 关键约束：MP **无法在运行时识别**对方是不是网页版

| 核查项 | 结果 |
|---|---|
| 两端 `_clientId` 生成代码 | **逐字符相同**（`'c' + Math.random().toString(36).slice(2,10) + Date.now().toString(36)`） |
| 是否区分端来源 | ❌ 不区分（web `:517` / MP `:220`） |
| GoEasy appkey | **两端共用同一个** `BC-58fd21e3...`，同一频道命名空间 |

**推论**：
1. **无法**用代码判断对端是网页版还是老版本 MP → 不能用"针对性放行/拒绝"来做条件逻辑。
2. **无法**只封禁网页版而不影响小程序 —— 共用 appkey 与频道，撤销即双端全断。
3. 因此「网页版下线」是一个**线下事件**，MP 代码**感知不到**。
   §六 的退出条件只能由**人工确认调度**，不能由程序自动收敛。

---

### R3（2026-09-12 20:04）— 用户决策：放弃网页版，仅做小程序端

用户决策：

> 「我考虑不更新网页版了，只做小程序。」

**直接收益（三项简化）**：

1. §五「两端对称核对」→ **单端**；实施量约减半
2. 两条网页端专有降级点直接消失：**多标签页共享同一令牌**（原决策 2A）、**无痕模式令牌丢失**
3. §九 发布顺序中的「网页端先」消失，只剩小程序审核发版

**但引入一项新的、更重要的约束**：

网页版将**冻结在 v1.3.8，永久不携带令牌** → §六 的兼容宽限期
**不再有自然退出条件**（原稿假定"两端都升到 v1.4.0"即可收紧）。
只要房间里有一端是网页版，**座位令牌机制整体降级为 v1.3.8 行为**，
**连原本较强的黑座一起降级**。

→ 已在 §六 标注。**需用户确认**：网页版是「冻结但继续可用」还是「即将下线」，
两种选择对应不同的兼容策略。

**联动收益**：服务端路线（§十一）的**最大技术难点** ——
「网页端需匿名/自定义登录，额外改造」 —— 随 R3 消失。
MP 的 `openid` 是天然可信身份锚点，见 §11.7。

---

### R2（2026-09-12 19:34）— 用户追问「是否考虑了白方掉线」暴露的设计缺口

用户追问：

> 「你的方案中是否考虑了白方掉线的情况？」

**没有专门处理，且这是个真实缺口。** 原稿的守卫条件写作
`requesterSeat === oppColor()`（沿用 v1.3.8），表面上对 white/black 对称，
但**「凭证从哪来」这一步是不对称的**，原稿完全没审视。

1. **白方掉线与黑方掉线同样危险，而且更隐蔽**（实验：`repro-white-seat.js`）：
   新人抢**黑座**时，`requesterSeat='black'`，对白方而言 `reqIsMyOpponent=false`
   → 至少会走到 v1.3.8 那个（失效的）面板分支。
   新人抢**白座**时，`requesterSeat='white'`，对黑方而言 `reqIsMyOpponent=true`
   → 命中「就是我对手」分支 → **连面板都不弹，直接发残局**。实测已确认。
2. **凭证获取渠道不对称**（原稿的致命遗漏）：
   - 保护**黑座** → 白方需持黑方凭证 → 黑方确认身份时发的 `request_sync`，白方必得 ✅
   - 保护**白座** → 黑方需持白方凭证 → 白方的 `request_sync` 在**建房时就发了**，
     黑方那时还没订阅（pubsub 不重放历史）→ **收不到** ❌
3. **连带推翻了 §7.2 第 4 项的可简化判断**：
   若按原稿倾向「只给 `request_sync` 带凭证」，白座就永远进 TOFU 分支
   → 把新人的凭证误采信为白方的 → **白座保护等于没做**。
   `sync`/`move` 带凭证**不是可选项**，是白座保护的**唯一可靠来源**。

---

### R1（2026-09-12 19:20）— 用户质疑后的重大修订

用户质疑：

> 「你说 1.3.8 线上无实际受害案例，只是因为没怎么测试。现在代码都没有一个机制确认回来的人
> 是不是掉线的那个人，那这个功能还有作用吗？」

**用户是对的，且原本的评估结论有严重方法论错误。**

1. **「无实际受害案例」的论证无效**。这是把「没被测到」当作「不存在」。
   正确表述应为：**该路径从未被测试覆盖**，因此「有无受害」**根本无数据**，不能作为不紧急的依据。
2. **漏洞已被实验证实**（`repro-silent-takeover.js`，见 §1.2）。
3. **原方案过度设计**：原稿要新增 `seatToken` 字段 + 新存储 key + 三态状态机。
   实验证明 **`claimAt` 已经满足特征码所需的三个属性**（§2.5），
   缺口只是「接收端从不记录对手的 claimAt」。**改动量可大幅缩小。**

---

## 一、要解决的问题

### 1.1 现状：协议层无法区分两类人

`request_sync` 是对弈者「宣告我占这个座」的唯一消息。但对同一个座位，它有两个**完全同形**的来源：

| 场景 | 协议表现 | 现状处理 |
|---|---|---|
| 对手网络抖动后回来 | `sender='black'` 的 `request_sync` | 直接回残局 |
| **新人抢占了空出的黑座** | `sender='black'` 的 `request_sync` | **直接回残局** ← 危险 |

因为 v1.3.8 后守卫条件退化为 `requesterIsPlayer && !reqIsMyOpponent`，
而两者都满足「是白/黑座位」且「不是我对手」（它们**就是** `oppColor()`）。

### 1.2 具体漏洞：60 秒宽限期后静默继承残局

**该漏洞已被实验证实**（`WeChatProjects/.ci-secrets/repro-silent-takeover.js`）：

```
=== 场景：白方（我）与黑方已下 1.e4 e5，残局 2 步 ===

[A] 黑方掉线 60s 内
    seatLocked(black) = true  → 判"空"？ false
    结果： ✅ 新人只能观战（保护有效）

[B] 黑方掉线超过 60s
    seatLocked(black) = false → 判"空"？ true
    结果： ❌ 可被占（保护失效）

[C] 新人以 sender="black" 发 request_sync（与"原主回来"同形）
    弹确认面板？ false
    是否收到残局？ 是 —— 8 字符
    结论： ❌ 静默继承成功（无任何身份校验）

=== 对照：原主自己回来，路径完全相同 ===
    弹确认面板？ false
    是否收到残局？ 是
    → 两次消息除 identity 外无任何差别，接收端无从区分。
```

复现步骤：

1. 黑方（米妈）掉线，心跳停止
2. 60s 后 `seatExpired('black')` 为真 → `doJoinRoom` 的 `seatTaken('black')` = `_roomColors['black'] || seatLocked('black')` = `false`
3. 新人（小米米）进房 → 判为空位 → 作为黑方加入
4. 新人发 `request_sync`（`sender='black'`）→ 白方认作「对手回来了」→ **直接发整盘残局**
5. 新人顶替黑座位，静默继续下

**关键**：这条路径**不触发任何确认面板**。`recv-takeover` 面板自 v1.3.8 后已无触发路径（死代码）。

### 1.2.1 房主端到底能察觉什么？（实验补充）

```
[D] 新人接手后，房主可见的变化有哪些？
    对手身份栏变化： 👩🏻 米妈 → 👧🏻 小米米     ← 唯一的"信号"：名字变了
    弹确认面板？ false
    有任何提示(notice)？ 无

[D2] 若新人选用【同一个身份名】呢？
    对手身份栏变化： 👩🏻 米妈 → 👩🏻 米妈     ← 完全无变化
    弹确认面板？ false
    → 房主连"名字变了"这个最后的信号都没有。
```

**结论：最坏情况下房主端零信号。** 新人只要沿用上一个玩家的身份名
（「米妈」这种预设名，本就只有三个选项，撞名概率不低），
房主看到的对手身份栏毫无变化 —— **没有任何途径察觉座位换了人**。

> ⚠️ **测试覆盖缺口（必须记录）**：座位租约的 N 段/M 段探针测的是
> 「宽限期内座位被锁」「换房间清租约」「非对弈座位不直接给残局」，
> **唯独没有测「宽限期过后新人进房」这一条**。
> 因此本漏洞长期存在却未被发现 —— 不是「无受害」，而是「无观测」。
> 新增的 S 段断言必须覆盖它（见 §八）。

### 1.3 目标与定位

**目标**：让接收端能区分「座位原主回来了」与「新面孔占了空位」。

> ⚠️ **定位：这是「防误占」，不是安全机制。**
> GoEasy 频道是公开的、纯前端 P2P，任何人订阅后都能伪造任意消息（包括伪造令牌）。
> 防御恶意冒充需要**服务端校验**，当前架构做不到。
> 本方案防的是：家人之间误操作、自己开另一个窗口、房间号被顺手传出去后对方进来 ——
> 这类**非恶意**的座位抢占。

---

## 二、令牌定义

### 2.0 术语约定（R1 后新增，用于消解全文摇摆）

下文出现的「**座位凭证**」是一个**中性术语**，指「证明某个座位属于我」的凭据。
它的**具体字段取决于选用的方案**：

| 方案 | 凭证字段 | 存储 | 状态 |
|---|---|---|---|
| **A（§2.5 推荐）** | **复用** `payload.claimAt` | 新增映射 `_seatClaimAt`，持久化到独立 key | ✅ 实验已证实可行 |
| **B** | 复用 `claimAt`，仅**首局**判定 | 同 A | 治标，保护只在一局内有效 |
| **C** | **新增** `payload.seatToken` | 新增 `_mySeatToken` + 独立 key | 原稿方案，改动大 |

> **阅读约定**：§三 ~ §七 的正文与示例代码为**可读性**统一写作 `seatToken` / `_seatTokens`。
> 若最终选 **A 或 B**，请把 `seatToken` **读作** `claimAt`、`_seatTokens` **读作** `_seatClaimAt`。
> **校验算法、状态机、探针断言与字段名无关** —— 换字段不改逻辑。
> 唯一的差异在 §2.5 指出的假阳性处理（撞名重选使 `claimAt` 更新），方案 C 无此问题。

### 2.1 格式与生成

```js
// 与 _clientId 同形，但语义完全不同（见 2.3）
function genSeatToken() {
    return 't' + Math.random().toString(36).slice(2, 10) + Date.now().toString(36);
}
```

- 随机 8 位 + 时间戳 base36 → 碰撞概率可忽略（家庭场景足够）
- 前缀 `t` 便于日志区分

### 2.2 生成时机：**`confirmRolePicker()` 内，不是在 `joinRoom()`**

| 时机 | 可否 | 理由 |
|---|---|---|
| `joinRoom()` | ❌ | 此刻 `myColor` 是脚手架值 `'black'`，座位**还没确定** |
| `resumeGame()` | ❌ | 从存储读取，不重新生成（否则原主认不出自己） |
| **`confirmRolePicker()`** | ✅ | 座位在此刻**真正提交**，令牌与座位同时诞生 |

> 这一条直接继承 v1.3.7 的教训：**不要把令牌绑到脚手架状态上**，
> 否则又会出现「未定座位的人带着假身份广播」。

### 2.3 与 `_clientId` 的本质区别

| | `_clientId` | `seatToken` |
|---|---|---|
| 生成频率 | **每次页面加载**重新生成 | 每个座位生成**一次**，跨会话不变 |
| 持久化 | ❌ 不持久化 | ✅ 持久化到独立 key |
| 用途 | 撞名时比大小 | 证明「这个座位是我的」 |
| 生命周期 | 页面存活期 | 直到显式退房 |

**`seatToken` 可以复用 `_clientId` 的值吗？不可以。** `_clientId` 带 `Date.now()` 且每次 `onLoad` 重建，
刷新页面就变了 —— 这正是它不能当凭证的原因（见审计结论）。

### 2.4 存储：独立 key，**不随 `chessSave` 清除**

```js
var SEAT_TOKEN_KEY = 'chessSeatTokens';
// 结构：{ "1234": { white: "t..", black: "t..", ts: 1789... } }
```

> ⚠️ **这是本方案最关键的一条约束。**
> `joinRoom()` 开头会 `removeItem('chessSave')`（web `index.html:799` / MP `index.js:1027`），
> 本意是「换房间别带上个房间的存档」。**但重连者恰恰就是重新输入同一个房间号** ——
> 若令牌存进 `chessSave`，它在能用之前就被清掉了。
> 故令牌必须独立存放，`chessSave` 的任何清理都不得波及它。

**剪枝**：`chessSeatTokens` 按 `ts` 保留最近 **3** 个房间，防止无限增长。

**清除时机**：仅 `leaveRoom()` / `_leaveRoom()`（显式退房 / 取消选身份）时，
删除**该房间**的令牌条目。

| 动作 | 令牌 | 理由 |
|---|---|---|
| 刷新页面 / 关闭标签页 / 小程序 `onUnload` | **保留** | 重连需凭证；这些钩子只停心跳、退订，不调 `leaveRoom()` |
| 显式退房 / 取消选身份 | **清除** | 「我不占这个座位了」是明确表态；同设备换人也靠这条兜住 |

### 2.5 ★ 重要发现：`claimAt` 已可充当特征码（实验证实）

**实验脚本**：`WeChatProjects/.ci-secrets/verify-claimat.js`

原稿设计了一套全新的 `seatToken`。但实验证明 **`claimAt`（v1.3.2 为撞名裁决引入的字段）
天然满足特征码所需的三个属性**，无需另造一个：

| 属性 | 要求 | 实测结果 |
|---|---|---|
| ① 可广播 | 接收端拿得到 | ✅ `broadcast()` 第 653 行已注入 `payload.claimAt = _claimAt` |
| ② 可恢复 | 原主重连时值**不变** | ✅ `resumeGame()` 里 `_claimAt = savedData.claimAt`，且 `chessSave` 存了这个字段 |
| ③ 因人而异 | 新人占位时是**新值** | ✅ `confirmRolePicker()` 里 `_claimAt = Date.now()`，与老值必然不同 |

**当前唯一的缺口**：接收端**从不记录**对手的 `claimAt`。

```
收到黑方 request_sync（带 claimAt）后：
  _playerIdentities = {"black":"米妈"}     ← 只记了名字
  → 是否记录了对方 claimAt？ ❌ 没有       ← 这就是缺口
```

**⚠️ 但 `claimAt` 有一个假阳性风险（实验发现）**：

```
④ 撞名让位后重选身份，claimAt 会变吗？
   forceRepickIdentity() 后 _claimAt = 0 （重置为待重选）
   重选确认后 _claimAt = 1789212147609
   → ⚠️ 变了！对方若记录了旧值 → 误判为"换了人"
```

场景：我选了「米妈」，对方也选「米妈」，撞名裁决让我让位 →
我重选成「小米米」→ `_claimAt` 更新为新时刻 →
**对方若已记录我的旧 `claimAt`，会把我的重选误判成"座位换了人"**。

**处理办法（三选一）**：

| 方案 | 做法 | 评价 |
|---|---|---|
| A | 撞名让位时，**主动广播**新 `claimAt`，接收端以「带 `claimAt` 的最新消息」为准更新记录 | ✅ **推荐**：`request_sync` 本就在重选后补发（`confirmRolePicker` 内），只需接收端在**已验证的座位**上允许更新 `claimAt` |
| B | 只用 `claimAt` 做**首次**判定，之后不再比对 | 治标：保护只在第一局有效 |
| C | 另造 `seatToken`（原稿方案） | 改动大，但彻底避开拓宽 `claimAt` 语义的风险 |

**修订后的倾向：方案 A** —— 复用 `claimAt`，把「记录对手 claimAt」这一个缺口补上。

> **为什么推荐 A 而不是 C**：`claimAt` 的语义是「我确认这个座位身份的**时刻**」，
> 而「座位换人」必然伴随一个新的确认时刻 —— **两者是同义的**。
> 另造 `seatToken` 会产生两个语义重叠的字段，长期看是维护负担。
> 风险是 `claimAt` 被赋予第二职责，需在 CONSISTENCY 里写清「它同时承担撞名裁决与座位校验」。

**方案 A 的实现要点**：

```js
// 接收端新增：记录对手的 claimAt（TOFU）
var _seatClaimAt = {};   // { white: 1789..., black: 1789... }

// 在 request_sync / move / sync 分支（已验证座位）：
if (data.claimAt) _seatClaimAt[data.sender] = data.claimAt;
```

**关键**：对**已认证的座位**，允许 `claimAt` 被更新（覆盖旧值）——
这正是方案 A 处理撞名重选的机制。而「换人」与「重选」如何区分？

> **无法区分，但无需区分**：两者都意味着「原主的座位身份发生了变更」。
> 在**有残局**时，两者都应该由房主确认（重选身份后继续下棋，确认一次是可接受的成本）。
> 在**空盘**时，两者都直接放行（无残局可抢）。
> **这个简化消除了原稿的三态状态机，只需「有残局 + claimAt 变了 → 弹面板」。**

---

## 三、协议变更

### 3.1 新增字段

在 `broadcast()` 注入，与 `clientId` / `claimAt` 并列：

```js
payload.seatToken = _mySeatToken;   // 未定座位时为空串
```

**携带范围**（只加这三类，最小面）：

| 消息 | 携带 | 作用 |
|---|---|---|
| `request_sync` | ✅ | **主校验点** —— 占座宣告 |
| `sync` | ✅ | 辅助 TOFU 记录（对方首次占座若只发 sync） |
| `move` | ✅ | 辅助 TOFU 记录 |
| 其他（`ping`/`room_*`/`emoji`/undo 系列） | ❌ | 不参与座位校验，避免面铺太大 |

> 为什么不给 `ping` 加：心跳是**座位已确立后**的维持信号，不是占座宣告。
> 加了会让「令牌错误」在心跳里反复触发，无处弹面板。校验集中在一个点更好推理。

### 3.2 不新增消息类型

复用 `request_sync` + 新字段，理由：
- 老版本客户端收到未知 `type` 会忽略，但**新版本收到老版本的 `request_sync` 仍需正确降级**（见 §六）
- 复用现有消息 = 老版本至少能走到「无令牌」分支，行为可预期

### 3.3 面板需携带待生效令牌

`recv-takeover` 面板确认后要把新令牌写进记录，故 `showPrompt` 需扩展：

```js
showPrompt({
    mode: 'recv-takeover',
    sender: requesterSeat,
    seatToken: data.seatToken,     // ← 新增：待生效的新令牌
    title: '有人想接手这局棋',
    ...
});
```

`resolvePrompt(true)` 的 `recv-takeover` 分支中：`_seatTokens[sender] = _promptSeatToken; saveSeatTokens();`

---

## 四、校验算法（接收端）

### 4.1 状态定义

对**每个座位**（white / black），接收端维护三态：

| 状态 | 含义 |
|---|---|
| `unknown` | 从未见过该座位的令牌 |
| `verified` | 已记录令牌，且本次令牌**匹配** |
| `foreign` | 已记录令牌，但本次令牌**不匹配** |

### 4.2 状态转移（收到带令牌的座位消息）

```
S = data.sender, T = data.seatToken
若 S 不是 white/black            → 不参与（观战者/未知）
若 S === 我的座位                → 忽略（自己的回声）
若 !T（老版本或未带）             → 保持原状态，跳过校验
若 _seatTokens[S] 不存在          → 记下 T（TOFU），state = verified
若 _seatTokens[S] === T          → state = verified
否则                              → state = foreign
```

**TOFU = Trust On First Use（首次见到即采信）**：座位第一次出现令牌时直接记录。
因对弈必须先收到对方消息，`_seatTokens[oppColor()]` 在正常对局中**必有记录**。

### 4.3 动作

| 状态 | `request_sync` 且有残局 | `request_sync` 且空盘 | `move` / `sync` |
|---|---|---|---|
| `verified` | 直接回残局（原主回来，**不打扰**） | 同上 | 正常处理 |
| `foreign` | **弹 `recv-takeover` 面板**，不直接发残局 | 接受换人并**更新令牌**（空盘无残局可抢） | 正常处理（见 §7.2 未决） |
| `unknown`（无令牌） | 直接回残局（向后兼容，§六） | 同上 | 正常处理 |

**为什么 `foreign` + 空盘要直接接受换人**：无残局可继承时，「谁占这个座位」没有利益冲突
（双方都从初始局面开始）。此处若弹面板，会让「前一局结束后换人对弈」这种正常流程平白多一次确认。
接受的同时**必须更新令牌**，否则该座位会永久停在失配状态，等下一局有棋局时误弹。

### 4.4 为什么只守 `request_sync` 这一处

新人进房的完整路径**必然**经过占座宣告：

```
joinRoom → room_check → doJoinRoom → showGameUI → openRolePicker
        → confirmRolePicker → broadcast(request_sync)   ← 必经点
```

`needIdentityPick` 为真时既不发心跳、也不发 `move`（两端均已拦截），
故新人在 `request_sync` 之前**无法落下任何一步**。守这一个点即可拦住。

> **⚠️ R2 更正：这个论证只证明了「校验点选得对」，没证明「凭证能拿到」。**
> 它守的是**新人**这一侧（新人的第一个座位消息必是 `request_sync` ✅），
> 但**没有回答**「接收方在那一刻手上有没有对方的凭证」。
> 对**白座**而言答案是否定的（§7.2-4 / §7.3）：黑方收不到白方建房时的
> `request_sync`（那时还没订阅），凭证只能靠 `sync`/`move` 建立。
> 也就是说：**校验点只有一个，但凭证来源必须有两个**（`request_sync` + `sync`/`move`）。
> 这个区别是本方案最容易踩空的地方 —— 单看 §4.4 会误以为"只守一处"就等于"只传一处"。

### 4.5 代码落位（替换 v1.3.8 的判据）

```js
// request_sync 分支（web index.html:1218 / MP index.js:500）
// 前置（沿用 v1.3.8，语义不变）：观战者一律不应答，先 return
var requesterSeat = data.sender;
var iAmPlayer = !isSpectator;
var requesterIsPlayer = requesterSeat === 'white' || requesterSeat === 'black';
var hasHistory = game.history && game.history().length > 0;

if (iAmPlayer && requesterIsPlayer) {
    var known = _seatTokens[requesterSeat];
    if (!data.seatToken) {
        // (a) 老版本对端：无从校验 → 放行（§六），不改记录
    } else if (!known) {
        // (b) TOFU：首次见到该座位 → 直接采信
        _seatTokens[requesterSeat] = data.seatToken;
        saveSeatTokens();
    } else if (known !== data.seatToken) {
        // (c) ★ 唯一新增能力：座位换了人
        if (hasHistory && needIdentityPick !== true) {
            // 有残局可继承 → 必须由我确认
            showPrompt({ mode: 'recv-takeover', sender: requesterSeat,
                         seatToken: data.seatToken, ... });
            return;
        }
        // 空盘（无残局可继承）→ 无人可抢，直接接受换人，并更新令牌。
        // 不更新的话，对方每次进房都会走到 (c)、反复触发无意义比对。
        _seatTokens[requesterSeat] = data.seatToken;
        saveSeatTokens();
    }
}
updateStatusText();
broadcast({ type: 'sync', ... });
```

**为什么 (c) 空盘分支必须更新令牌**：若只判不更，则「对方先占座→退房→换人占座」在
空盘上会永远停在 `known !== incoming` 状态。虽不弹面板（因 `!hasHistory`），
但记录永久失配，一旦之后有棋局就误弹。**代价是空盘换人无法被守护** ——
但空盘本就无残局可抢，守护无意义。

> 对比 v1.3.8：`!reqIsMyOpponent` 判据**被令牌比对取代**。
> 在 v1.3.8 里「请求方不是我对手」是唯一的排除依据，而它无法区分
> 「原主回来」与「新人抢位」（两者都**是** `oppColor()`）。
> 令牌比对把这个问题从「猜」变成「验」，**并让 `recv-takeover` 面板重新有了触发条件**。

### 4.6 与 v1.3.8 的关系（不是叠加，是替换）

| | v1.3.8 | v1.4.0 本方案（**R4 确认**） |
|---|---|---|
| 判据 | `requesterIsPlayer && !reqIsMyOpponent` | `requesterIsPlayer` + 令牌比对 |
| 能区分原主/新人吗 | ❌ 不能（这正是面板变成死代码的原因） | ✅ 能 |
| 面板触发条件 | 无（死代码） | 令牌不匹配且有残局 |

`requesterIsPlayer` **保留**（排除观战者，v1.3.8 的成果，仍然必要）；
`!reqIsMyOpponent` **删除**（被令牌比对取代，且它已无区分能力）。

> ✅ **R4 确认：本节结论正确。** R3 曾一度标注「不可删除」，经逐行核对为**误判**，已撤回。
> 详见 §零 R4 与 §5.2（含穷举证明：该条件在"抢空位"场景中恒为 false）。

---

## 五、可行性核对（R3 后：仅小程序端）

> **R3 变更**：用户决定**不更新网页版**。下表保留网页端一列仅作**冻结基线记录**
> （说明"为什么网页端不需要再动"），**实现范围只有小程序端**。
> 原「两端必须逐条对称实现」的要求（§5.1）**已取消**。

| 维度 | 网页端（**冻结于 v1.3.8**） | 小程序端（**本次实现范围**） | 结论 |
|---|---|---|---|
| 持久化 API | `localStorage`（同源） | `wx.setStorageSync`（按 appid） | ✅ |
| 刷新保留 | ✅ 刷新不清 localStorage | ✅ 无刷新概念，`onLoad` 重建但 storage 在 | ✅ |
| 关闭后重进 | ✅ 保留 | ✅ 保留 | ✅ |
| 显式退房清除 | `leaveRoom()` | `_leaveRoom()` | ✅ |
| **多标签页** | ⚠️ 共享 localStorage → 两标签持同一令牌 | ✅ 无此概念 | ✅ 小程序端**无此问题**（原决策 2A 作废） |
| 无痕/隐私模式 | ⚠️ 关闭即清 | — 无等价物 | ✅ 不适用（该降级点消失） |
| 跨端切换 | 见 §五-A | 同左 | ⚠️ **仍存在，且不降级** |
| 探针桩 | — 不再新增 | ✅ `probe-index-page.js:67` 有 `store` 桩 | ✅ 仅 MP 需新增 S 段 |

### 5.1 小程序端实现清单（原「两端对称」已作废）

| # | 规则 | MP 落位 |
|---|---|---|
| 1 | `genSeatToken()` | 工具函数区 |
| 2 | `SEAT_TOKEN_KEY` 独立 key | 常量区 |
| 3 | 令牌在 `confirmRolePicker()` 生成 | 同函数（`:827` 附近） |
| 4 | `resumeGame()` 从存储读回 | 同函数（`:1118` 附近） |
| 5 | `broadcast()` 注入令牌 | 与 `clientId` 并列（`:421` 附近） |
| 6 | TOFU 记录 + `foreign` 弹面板 | `request_sync` 分支（`:525` 附近） |
| 7 | 同意接手后更新令牌 | `resolvePrompt` |
| 8 | `_leaveRoom()` 清除该房间令牌 | 同函数 |
| 9 | 保留最近 3 个房间 | 剪枝函数 |

**改动量**：`miniprogram-1/pages/index/index.js` 单文件，预估 ~50 行。
**`docs/index.html` 不再改动。**

### 5.2 ★ R4 重写：`!reqIsMyOpponent` **必须删除**（R3 的"保留"结论已撤回）

> **本节在 R3 时写作「不可删除」，R4 逐行核对后确认写错了，已重写。**
> 保留原错误记录于 §零 R4，以免后人重蹈。

**先看 v1.3.8 原始判据**（`docs/index.html:1243-1259` / MP `:525` 附近）：

```js
var requesterSeat = data.sender;
var requesterIsPlayer = requesterSeat === 'white' || requesterSeat === 'black';
var reqIsMyOpponent  = iAmPlayer && requesterSeat === oppColor();
if (iAmPlayer && hasHistory && requesterIsPlayer && !reqIsMyOpponent && ...) {
    // 弹 recv-takeover 面板
}
```

**穷举（只有两个对弈座位）**：

| 请求方座位 | 我的座位 | `reqIsMyOpponent` | `!reqIsMyOpponent` | 结果 |
|---|---|---|---|---|
| 我的座位（`myColor`） | 白/黑 | false | **true** | 面板触发 |
| **空出的对手位（`oppColor()`）** | 白/黑 | **true** | **false** | **面板不触发** |

**「抢空位」场景中，请求方坐的必然是空出来的那个位子 = `oppColor()`**
（我占着我的位子，另一个位子空了才谈得上"抢"）→ 恒为第 2 行 → **面板恒不触发**。

→ 结论：`!reqIsMyOpponent` **是死条件**。这正是 §4.6 判其为死代码、
§1.2（`repro-silent-takeover.js`）实测「静默继承残局」的**同一根因**。

> **可执行证据**：`WeChatProjects/.ci-secrets/verify-guard-constant.js`
> （6 passed / 0 failed）穷举两个座位方向，证明抢空位场景下该守卫**恒为 false**。
> 与 `repro-silent-takeover.js`（行为证据）、`repro-white-seat.js`（白座证据）并列。

**因此**：

| 做法 | 后果 |
|---|---|
| **删除**（§4.6 原指） | MP↔MP 由令牌比对接管 ✅；MP↔网页版走"缺令牌=放行"❌ 但**与 v1.3.8 现状持平，无回归** |
| ~~保留~~（R3 误判） | **MP↔MP 的面板也永不触发** ❌ 保护**整体归零**，比删掉更差 |

**→ `!reqIsMyOpponent` 必须删除。** 保留它**不会**为网页版场景提供兜底，
只会把本可修复的 MP↔MP 场景一起拖死。

**网页版场景的无保护状态，无法靠任何旧守卫改善** ——
它是"对方不带令牌"直接导致的，唯一解是**下线网页版**（§六 分支 B）。

---

## 六、向后兼容矩阵（**R3 后：不再有退出条件**）

**R3 前**的兼容对象是「尚未更新的老版本」，属于**临时过渡**。
**R3 后**，网页版是**永久的老版本** —— 性质从"过渡期"变成"长期共存"。

| 我 \ 对方 | MP 新版本（带令牌） | MP 老版本（无令牌） | **网页版 v1.3.8（永久无令牌）** |
|---|---|---|---|
| **MP 新版本** | ✅ 完整校验 | ⚠️ 放行（无法校验） | ❌ **永久放行** |
| MP 老版本 | ⚠️ 忽略新字段 → 退回 v1.3.8 | ⚠️ 双方退回 v1.3.8 | ⚠️ 同左 |
| 网页版 v1.3.8 | ⚠️ 忽略新字段 → 退回 v1.3.8 | ⚠️ 同左 | ⚠️ 同左 |

**降级原则不变：缺令牌 = 放行（不做悲观判定）。**
理由仍成立：若把「无令牌」判成 `foreign`，网页版玩家每次进房都会被弹面板。

**★ R3 的核心影响：网页版那一列是"永远"的**（**R4 修订：改为"直到人工下线为止"**）。

只要房间里有一方是网页版，**座位令牌机制在本局内完全失效**：

- 不是"降级为较弱保护"，而是**整套机制不参与**（网页端既不发送也不校验令牌）
- 且**连原本较强的黑座保护一起失效**（§7.1 原记「黑座强、白座弱」的差异在此场景下无意义）
- **R4 补充**：这个场景**无法用代码改善** —— 两端 `_clientId` 生成逐字符相同（web `:517` / MP `:220`），
  MP **无法识别**对端是网页版还是老版本小程序；且共用同一 GoEasy appkey 与频道命名空间，
  无法只封禁网页版。故它只能靠**线下下线**消除。

### 6.1 R4：分支已定 = **B（网页版计划下线）**

用户确认：「网页版确定不管了。」→ **兼容期有明确终点**，令牌机制照做。

**但"下线"必须真做，否则 B 退化为 A**。当前**尚未下线**（实测 HTTP 200、v1.3.8）。

**下线检查单（建议照做，否则约束不会消失）**：

| # | 动作 | 说明 |
|---|---|---|
| 1 | 停用 `docs/index.html` 入口 | 保留源码，但让域名不可达 |
| 2 | GitHub Pages / 托管侧关闭 | `docs/` 是发布源，删文件即生效；`CNAME` 同步处理 |
| 3 | 确认 `chess.yiruifu.cn` 不再返回 200 | 用 `curl -o /dev/null -w "%{http_code}"` 复核 |
| 4 | **保留 GoEasy appkey 不动** | ⚠️ 两端共用，撤销会**连小程序一起断** |
| 5 | 公告/告知已用网页版的对手 | 避免对方以为服务故障 |

**退出条件（人工触发，程序无法自动收敛）**：

- 网页版域名不可达 **且** 已等待 ≥ 一个发布周期（覆盖"用旧版页面已打开但未关闭"的会话）
- 届时**另开版本**把「无令牌」升格为 `foreign` 或直接告警

**待确认分支**（保留备查，已选 B）：

| 分支 | 含义 | 对应策略 |
|---|---|---|
| A. 网页版长期保留 | 兼容期无尽头 | 令牌机制收益需重估（挡不住任何网页版场景） |
| **B. 网页版计划下线** | **✅ 已选** | 令牌机制照做；下线后解锁 §6.1 检查单 |
| C. 网页版禁入有令牌的房间 | 最彻底 | 双向拒绝，但**技术上不可行**（R4：无法识别端来源 + 共用 appkey） |

---

## 七、已知限制与降级

### 7.1 明确接受（决策已定）

| 限制 | 影响 | 决策 |
|---|---|---|
| ~~网页端多标签页持同一令牌~~ | — | ✅ **R3 后消失**（不再改网页端） |
| ~~无痕模式令牌丢失~~ | — | ✅ **R3 后消失**（同上） |
| 跨端切换被判陌生人 | 换设备需对方确认一次 | **3：可接受**（换设备本就该确认） |
| 房主自己也丢了记录 | TOFU 会重新采信首个令牌 → 守护降效 | 已有缓解：令牌随独立 key 保存，`onLoad` 后从本地恢复 |
| 双方同时掉线后新人进来 | 新人可能被 TOFU 采信 | 罕见；属"两端都无记录"的盲区 |
| **黑方换设备 / 清缓存** | **白座保护降级**：黑方无凭证，对白座走 TOFU | **R2 新增**。缓解：见 §7.3 |
| **房间内有网页版玩家** | **整套令牌机制失效**（含较强的黑座） | ⚠️ **R3 新增**，见 §六。已选 **B：计划下线**（§6.1 检查单） |
| 非恶意假设 | 恶意者可伪造令牌绕过 | **定位即如此**（§1.3），需服务端才能根治 |

### 7.2 未决（实现前需定）

1. **`foreign` 状态下收到 `move` / `sync` 怎么办？**
   当前设计只守 `request_sync`。若新人被拒后仍强行发 `move`（改客户端），是否要丢弃？
   - 选项 A：不处理（依赖 §4.4 的「必经点」论证）—— **倾向**
   - 选项 B：`foreign` 座位的 `move`/`sync` 一并丢弃
   - 反方理由：B 会让「房主刷新后 TOFU 记录丢失 → 误判 foreign」变成**丢步**，
     后果比"多下几步"严重得多。风险不对称，故倾向 A。

2. **被拒绝后新人的令牌如何处置？**
   新人转观战 → 应清掉自己 `_mySeatToken`，且**不写入**存储。需在实现时确认这条。

3. **「对手本人回来了」但对方设备换了**（清过缓存 / 换手机）
   → 令牌不同 → 弹面板。这是**正确行为**（确实换了设备），但文案是否要区分？
   如：「有人想接手这局棋」vs「对手换了设备，请求继续」—— 当前协议**无法区分**这两种情况，
   故文案统一。若要区分，需对方额外上报「我不是新人，我有旧令牌但丢了」之类的自述，不可信，不建议。

4. **`sync` / `move` 的「辅助 TOFU 记录」是否真有必要？** → **R2 已定：必须保留，且是硬需求**
   ~~§3.1 给这两类也带了令牌。但按 §4.4 的必经点论证，新人的第一个座位消息**必然**是
   `request_sync`，TOFU 在那里就能建立。给 `move`/`sync` 加令牌的收益存疑，
   而成本是每步棋多 1 个字段。**实现时可简化为只给 `request_sync` 带令牌**（更小面）。~~
   ~~保留在方案里的理由：对端是**老版本**时…~~
   
   **R2 更正（见 §零 R2）**：上述「原稿倾向」只对**黑座**成立，对**白座会直接失效**。
   
   §4.4 的必经点论证有个未声明的隐含前提：**双方都收得到对方的 `request_sync`**。
   实际上：
   
   | 要保护的座位 | 接收方 | 需持有的凭证来源 | 该来源可靠吗 |
   |---|---|---|---|
   | 黑座 | 白方（房主） | 黑方的 `request_sync`（黑方进房时发） | ✅ 白方此时已在房间 |
   | **白座** | 黑方 | 白方的 `request_sync`（**建房时**就发了） | ❌ 黑方那时**还没订阅** |
   
   → 黑方对白座的凭证，**只能**从 `sync`（白方应答黑方 request_sync 时必发）
   或 `move`（白方执先，第一步必发）建立。
   
   **结论：`sync` / `move` 必须带凭证。** 若砍掉，白座将永远停在 TOFU 分支，
   把新人的凭证采信为白方的 → **白座保护形同虚设**，且是**静默失效**（不报错、无异常）。

---

### 7.3 ★ R2 新增 / R3 修订：白座的保护强度天然弱于黑座（需明确接受）

即便按 §7.2 第 4 项让 `sync`/`move` 都带凭证，白座保护仍有一个**结构性弱点**：

**黑方无凭证（换设备 / 清缓存 / 重装小程序）→ 对白座走 TOFU → 白座保护降级。**

> **R3 修订**：原稿写「黑方刷新页面」。小程序**无刷新概念**，
> 且凭证存独立 key（不随 `chessSave` 清除），`onLoad` 重建后即恢复 → 该路径**基本缓解**。
> 残余的真实风险是**换设备 / 清缓存 / 重装**（storage 真丢失），比"刷新"罕见得多。

**为什么黑座没有这个问题**：白方是房主，凭证在自己手上，
黑方重建后重新发 `request_sync`（带**自己保存的**凭证）→ 白方能比对 ✅。

**结论与定位**：

| 座位 | 保护强度 | 原因 |
|---|---|---|
| 黑座 | **强** | 白方（房主）在自己房间里积累黑方凭证，来源稳定 |
| 白座 | **较弱** | 黑方依赖白方主动发来的 `sync`/`move` 才能建立凭证 |

**取舍**：这**不是**设计缺陷，而是「无服务端」的固有代价。
白座保护覆盖的是「黑方在线、白方掉线、新人抢白座」这一常见场景（已覆盖），
遗漏的是「黑方自己也换了设备」这一罕见叠加场景。
**建议接受，并在 CONSISTENCY 里如实记录强度差异**，不要写成"完全对称"。

> **R3 后的小程序端验证点**：需用探针确认
> **`onHide`/`onShow` 往返后凭证不丢**（S14）。
> 小程序「切后台再回来」是高频操作，比网页版刷新更常见 —— 这是 R3 带来的**新验证重点**。

---

## 八、探针断言清单（S 段）

**R3 后只需在 MP 侧新增**（原「两端各新增 S 段」作废）：
落位 `miniprogram-1/` 的探针文件（原 `probe-index-page.js:67` 已有 `store` 桩）。

### 8.1 补覆盖缺口（最高优先）

| # | 断言 | 类型 |
|---|---|---|
| **S0** | **黑方掉线超 60s 后，新人进房必须被识别为"换了人"** | 动态 |
| **S0b** | **上述场景下若本地有残局 → 弹 `recv-takeover` 面板，且不发残局** | 动态 |
| **S0c** | **新人沿用【同一身份名】时也必须能识别（不能只靠名字变化）** | 动态 |

> S0 系列是本次实验暴露的**真实覆盖缺口**（见 §1.2）。
> 现有 N/M 段全部漏测「宽限期过后」这个时点，必须补齐。

### 8.2 令牌/凭证机制

| # | 断言 | 类型 |
|---|---|---|
| S1 | `joinRoom()` 清 `chessSave` 后，座位凭证（`chessSeatTokens` 或 `claimAt` 记录）**仍存在** | 存储 |
| S2 | 对手带**正确**凭证的 `request_sync` → 直接回 `sync`，不弹面板 | 动态 |
| S3 | 对手带**错误**凭证的 `request_sync` → 弹 `recv-takeover` 面板，且不发残局 | 动态 |
| S4 | 首次见到**未知**凭证 → TOFU 记录，不弹面板 | 动态 |
| S5 | 对方 `request_sync` **无**凭证字段 → 放行不弹面板（向后兼容） | 动态 |
| S6 | 同意接手后 → 凭证记录更新为新值 | 动态 |
| S7 | 拒绝接手后 → 凭证**不**更新 | 动态 |
| S8 | `leaveRoom()` → 该房间凭证记录被清除 | 动态 |
| S9 | 自己的凭证确随 `request_sync` 广播出去 | 动态 |
| S10 | 源码取证：凭证存储与 `chessSave` 是**不同 key**，且清理点不重叠 | 取证 |
| S11 | 凭证在 `confirmRolePicker()` 生成/提交（不是 `joinRoom()`） | 取证 |
| S12 | **撞名让位重选身份后，凭证更新能被对方接受（不误判为换人）** | 动态 |

### 8.3 ★ R2 新增：白座方向的覆盖（原清单只测了黑座视角）

> 原 S1–S12 默认站在「白方（房主）视角、新人抢黑座」这一条路上，
> **未区分接收视角**。R2 证明两个方向**不可互换**（§7.3），必须分列。

| # | 断言 | 类型 |
|---|---|---|
| **S13** | **白方掉线超 60s + 新人抢白座 → 黑方必须弹 `recv-takeover` 面板**（当前实测：静默发残局） | 动态 |
| **S13b** | **黑方视角：白方凭证可从 `sync` 正确建立**（黑方发 request_sync → 白方回 sync 带凭证 → 黑方记录） | 动态 |
| **S13c** | **黑方视角：白方凭证可从 `move` 正确建立**（白方执先第一步 move 带凭证） | 动态 |
| **S14** | **黑方刷新后凭证能否从独立 key 恢复**（验证 §7.3 的缓解手段是否生效） | 动态 |
| **S15** | **反向证明：若只给 `request_sync` 带凭证 → S13 必须 FAIL**（证明 §7.2-4 不可简化） | 反证 |

### 8.4 ★ R3 新增：MP 独有路径（网页端已无对应场景）

| # | 断言 | 类型 |
|---|---|---|
| **S16** | **`onHide` → `onShow` 往返后，凭证不丢**（小程序切后台/回前台；R3 新增的高频操作） | 动态 |
| ~~S17~~ | ~~对端为网页版 → 仍走 `!reqIsMyOpponent` 兜底可弹面板~~ | ❌ **R4 作废** |
| ~~S18~~ | ~~反证：若首版删除 `!reqIsMyOpponent` → S17 必 FAIL~~ | ❌ **R4 作废** |
| **S19** | **反证（R4）：对端无令牌时，删除 `!reqIsMyOpponent` 后 MP↔MP 的 S1–S3 仍全绿** —— 证明该守卫与 MP↔MP 保护**无关**（即它不是兜底，删了也不影响可修复场景） | 反证 |
| **S20** | **取证（R4）：`!reqIsMyOpponent` 在"抢空位"场景中恒为 false** —— 静态穷举两个座位，断言分支不可达（与 S15 同为"死条件"证明） | 取证 |
| **S21** | **取证（R4）：两端 `_clientId` 生成同形，且无法从协议字段判别对端端别** —— 证明"技术封禁网页版"不可行（§6.1 依据） | 取证 |

> **S17/S18 作废说明**：R3 时误以为可保留 `!reqIsMyOpponent` 兜底网页版场景；
> R4 证明该条件恒 false、保留只会拖垮 MP↔MP，故对应断言失去意义。
> 取而代之的 S19/S20 用来**固化这次的判断**（防止又被误改回去）。

**S13 的「先确认 FAIL」是强制步骤** —— 当前代码（v1.3.8）就该让它 FAIL，
这是验证「断言真的守住了这个洞」的必要前提（同下方反证要求第 3 条）。

**反证要求（强制，两轮）**：

1. 把凭证比对退回 v1.3.8 的 `!reqIsMyOpponent` 判据 →
   **S3 必须 FAIL**（错误凭证不再弹面板）、**S0/S0b/S0c 必须 FAIL**（漏洞重现）、
   **S13 必须 FAIL**（白座方向同样重现）。
2. 还原后全绿。
3. **S0 / S13 系列必须先在「修复前的当前代码」上跑一遍确认 FAIL** ——
   这是验证"断言真的守住了这个洞"的必要步骤（否则可能是恒真断言）。
4. **S15 单独做一轮**：把 `sync`/`move` 的凭证注入去掉，只留 `request_sync`，
   确认 **S13 由 PASS 转为 FAIL** —— 这是 §7.2-4 「不可简化」结论的实验依据。
5. **S18 单独做一轮（R3）**：首版即删除 `!reqIsMyOpponent`，
   确认 **S17 由 PASS 转为 FAIL** —— 这是 §5.2 结论的实验依据。
   > **R4 作废**（S17/S18 已删，见上）。替代：**S19 + S20 固化 R4 的纠正结论**。

---

## 九、实施影响面（R3 后）

| 文件 | 改动 | 预估 |
|---|---|---|
| ~~`docs/index.html`~~ | **不改动**（R3：网页版冻结于 v1.3.8） | 无 |
| `miniprogram-1/pages/index/index.js` | 令牌模块 + 校验分支 + 面板字段 + 存储剪枝 + **删除 `!reqIsMyOpponent`**（R4 确认可删） | 中 |
| `docs/CONSISTENCY.md` | 新增第十一节；§9.7 的"死代码"结论改写；如实记录（a）白/黑座强度差异（§7.3）（b）**网页版共存期令牌机制整体失效（§六）**（c）**网页版下线状态与日期（§6.1）** | 小 |
| `docs/SEAT_TOKEN_DESIGN.md` | 实现后删除，内容并入 CONSISTENCY | — |
| **MP 探针** | 新增 S1–S16、S19–S21（S17/S18 于 R4 作废） | 中 |
| **网页版下线** | 见 §6.1 检查单（**代码之外的动作**） | 小 |

**版本**：`1.4.0`（新协议字段，非热修复）。
版本号同步：**MP `APP_VERSION`（`index.js:87`）+ `CONSISTENCY.md`**。
~~web `APP_VERSION`~~ → **不动**（R3）。

**发布顺序**：仅小程序端，需审核发版。
**过渡期**：R3 后**无退出条件**，见 §六。

---

## 十、与「临时身份」方案的关系

用户此前另提过一个方案：给未定座位的新人分配专用的临时身份（`sender='joining'`）。
两者**同层但不同职责**，可并行：

| | 临时身份 | 座位令牌 |
|---|---|---|
| 解决 | 脚手架值被当成协议信号（v1.3.7 那类） | 座位换人无法识别（本方案） |
| 标识 | 「我**还没**座位」 | 「这个座位**是不是我的**」 |
| 现状 | v1.3.7 已用两条特判覆盖，**无已知残余风险** | 有真实漏洞（§1.2） |
| 优先级 | 低（可维护性收益，非正确性） | **较高**（有实际漏洞） |

**建议**：本方案优先。临时身份方案仍是 v1.4.x 的可选项 ——
它能把 v1.3.7 的两条特判（`touchSeat` 忽略己座 + `room_check` 不续租）
合并为一条结构不变量，但需改回声过滤根（`sender === myColor` → `clientId === _clientId`），
风险高于收益，**不建议与令牌方案同批做**。

---

## 十一、服务端可行性评估记录（2026-09-12，**评估结论，非决策**）

> 触发：用户新接入 CloudBase 连接器后提问「可以用这个做一个服务端吗？」
> 本节仅记录核查结果与分析结论，**尚未选定路线**，不改动本文 §一~§十 的任何方案。

### 11.1 结论

**可以，但作用域必须限定。**

- 能当「**权威裁决层**」：低频、强一致 —— 谁占哪个座、回来的是不是原来那个人、残局归谁。
- **不能**当「**传输层**」：高频落子广播继续留在 GoEasy。

### 11.2 核查事实

| 项 | 实际值 |
|---|---|
| 产品 | 腾讯云 CloudBase（云开发），非自建服务器 |
| 登录 | `READY`，**账号级**凭据，地域 ap-shanghai |
| 环境 | `xiong-1030-d5gef6w0o28479d9f`，NORMAL，默认环境，类型 baas |
| **关键巧合** | `Source: miniapp`，**WxAppId = `wx07175710bda602c0`** |
| 与项目关系 | 与 `miniprogram-1/project.config.json:30` 的 `appid` **完全一致** |

环境**本就由该小程序创建**，归属与权限天然打通。

可用资源：云函数（SCF）、文档型数据库（`tnt-dr9980x8a`）、
云存储（`7869-xiong-1030-...tcb.qcloud.la`）、
静态托管（`xiong-1030-...tcloudbaseapp.com`，路由已启用）。

### 11.3 与本文的关系：正好命中三处「无服务端则做不到」

| 本文原话 | 服务端后 |
|---|---|
| §1.3（`:134-137`）「GoEasy 频道是公开的…任何人订阅后都能伪造消息（含伪造令牌）。防御恶意冒充需要**服务端校验**，当前架构做不到」 | 凭证由云函数签发/校验，客户端降为转发者，**伪造失效** |
| §7.1（`:511`）「非恶意假设 —— 恶意者可伪造令牌绕过 …需服务端才能根治」 | 同上，该假设可撤销 |
| **R2 缺口**：凭证获取渠道不对称（黑方收不到白方建房时的 `request_sync`） | 权威状态在服务端，双方改为「问服务端」，**不再依赖谁曾收到过哪条广播** |

第三行价值最高：R2 缺口本质是「**没有共同真相，只能靠各自记忆**」。
一旦有更强的第三方持有真相，§7.2 第 4 项（`sync`/`move` 是否带凭证）的纠结会直接消解。

### 11.4 三条硬约束

1. **实时性不足，不能替代 GoEasy。** CloudBase `watch()` 官方明确不适用于
   每秒数十次的高频写入，会被合并/节流 —— 「不要把它当事件总线用」。
   落子延迟敏感，继续走 GoEasy。
2. **当前为体验版（`baas_trial`）。** 资源点计费（`EnvDeductionMode: credits`），
   免费 3,000 点/月，额度耗尽即停；到期 `2027-03-11`，`IsAutoRenew: false`。
   正式对外运行大概率需升级档位 —— **具体档位限制需在控制台二次核实，不下断言**。
3. **后端仅 NoSQL。** `RuntimeBackends {postgresql:false, mysql:false}`。
   座位表这种结构化数据用文档库可行，但事务/唯一约束需云函数自行保证。

### 11.5 工程现状

- 项目当前 **0 处 `wx.cloud` 引用**（小程序端与网页端均走 GoEasy）。
- **无 `cloudbaserc` / `cloudfunctions` 目录**，真要用需从零初始化。
- 工作区根目录存在**空目录 `5498bac8b7952859c81f8e4f568eaa46/`**（0 字节，15:34 创建），
  来源未明，待用户确认是否清理。

### 11.6 与 v1.4.0 的关系

服务端方案会与本文的**纯前端路线产生分叉**，二者不是叠加关系：

| | 纯前端令牌（本文 §一~§十） | CloudBase 权威层 |
|---|---|---|
| 防非恶意抢座 | ✅ | ✅ |
| 防恶意伪造 | ❌（§1.3 明确承认） | ✅ |
| 需改协议字段 | 是 | 可重新设计 |
| 依赖外部服务 | 无 | CloudBase（体验版额度） |
| 网页端可行性 | 天然可行 | 需匿名/自定义登录，**额外改造** |

### 11.7 ★ R3 联动：服务端路线的最大障碍已消失

**R3 前**，服务端方案要解决「网页版如何取得 CloudBase 身份」——
需引入匿名登录或自定义登录，是额外的鉴权改造，也是网页端能否接入的**唯一拦路石**。

**R3 后**（网页版不再更新），这条障碍**不存在了**：

| | R3 前 | R3 后 |
|---|---|---|
| 小程序端身份 | `openid`（可信） | `openid`（可信） |
| 网页端身份 | 需匿名/自定义登录 ❌ | **不需要了** ✅ |
| 鉴权改造量 | 两套 | **一套（仅 MP）** |

**MP 的 `openid` 是天然可信的身份锚点** —— 由微信平台签发、不可伪造，
比任何客户端生成的令牌都强。这正是服务端路线相对纯前端路线的**根本优势**：

- 纯前端：令牌由客户端 `Date.now()` 生成，**任何人可伪造**（§1.3 明确承认此局限）
- 服务端：`openid` 由云函数从调用上下文取得，**客户端无法伪造**

→ 若走服务端路线，v1.4.0 的「座位令牌」可被**更彻底地**替换为
「以 `openid` 为键的座位归属记录」，连 §7.2/§7.3 的一堆权衡都无需存在。

**但注意**：这**不意味着**应立即转向服务端。
R3 只是**移除了一个障碍**，并未改变 §11.4 的三条硬约束（实时性、体验版额度、仅 NoSQL）。
尤其**体验版额度**：永久在线服务依赖计费持续性，是比"改不改网页版"更硬的现实约束。

**未决**：先收 v1.4.0 纯前端方案，还是并入服务端一并做。
**建议若要动手，先做最小连通验证（云函数 + `seat_claims` 集合跑通读写），
再决定是否扩大**，避免直接改造两端鉴权。
